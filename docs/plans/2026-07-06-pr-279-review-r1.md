# PR 279 Review Round 1

## Scope

Address the accepted and partially accepted review feedback on PR #279 without reopening
D1 decisions.

## Tasks

1. Add a focused duplicate-name guard for skipped scheduled-start notifications:
   - Test that notification identifiers are keyed by scheduled profile UUID.
   - Implement the identifier helper and route `postSkippedStartNotification` through it.

2. Clarify intentional StrategyManager control flow:
   - Document that typed app-side guards run before the shared background-stop policy call.
   - Keep behavior unchanged.
   - Split defensive policy-result mapping so future readers do not infer that all cases are
     expected in the app-side path.

3. Verification:
   - Run the focused ScheduleTimerActivity test.
   - Run swift-format lint.
   - Push signed follow-up commit.
   - Reply to and resolve PR review threads according to the approved triage.
